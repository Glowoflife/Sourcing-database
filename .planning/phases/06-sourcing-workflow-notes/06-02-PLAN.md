---
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/app/(dashboard)/manufacturers/page.tsx, src/components/manufacturers/detail-header.tsx, src/components/manufacturers/detail-sections.tsx, src/components/manufacturers/results-table.tsx, src/components/manufacturers/search-toolbar.tsx]
autonomous: false
requirements: [CRM-03, CRM-04]
must_haves:
  truths:
    - "Users can update sourcing status from the detail page header via API."
    - "Notes can be added and viewed in the detail page, persisting via API."
    - "The manufacturers dashboard displays a Sourcing Status column."
    - "The manufacturers dashboard includes a faceted filter for Sourcing Status."
    - "The Manufacturers page handles sourcingStatus URL parameters."
  artifacts:
    - path: "src/components/manufacturers/detail-header.tsx"
      provides: "Status update UI wired to /api/leads/[id]/sourcing-status"
    - path: "src/components/manufacturers/detail-sections.tsx"
      provides: "Notes UI wired to /api/leads/[id]/notes"
    - path: "src/components/manufacturers/results-table.tsx"
      provides: "Table column for sourcing status"
    - path: "src/components/manufacturers/search-toolbar.tsx"
      provides: "Dashboard filter for sourcing status"
    - path: "src/app/(dashboard)/manufacturers/page.tsx"
      provides: "Propagates sourcingStatus from URL to query layer"
  key_links:
    - from: "src/components/manufacturers/detail-header.tsx"
      to: "/api/leads/[leadId]/sourcing-status"
      via: "fetch PUT"
    - from: "src/components/manufacturers/detail-sections.tsx"
      to: "/api/leads/[leadId]/notes"
      via: "fetch POST"
---

<objective>
Integrate Sourcing Workflow and Notes into the Manufacturers Dashboard and Detail UI.

Purpose: Enable users to interact with the sourcing workflow, qualify leads, and view status/notes across the application (CRM-03, CRM-04).
Output: Updated UI components with real-time status management, note persistence, and enhanced dashboard filtering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/app/(dashboard)/manufacturers/page.tsx
@src/components/manufacturers/detail-header.tsx
@src/components/manufacturers/detail-sections.tsx
@src/components/manufacturers/results-table.tsx
@src/components/manufacturers/search-toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail Page Integration (Status & Notes)</name>
  <files>src/components/manufacturers/detail-header.tsx, src/components/manufacturers/detail-sections.tsx</files>
  <action>
    1. Update `src/components/manufacturers/detail-header.tsx`:
       - Add a dropdown or toggle group to change `sourcingStatus`.
       - **Wire to API**: Use `fetch` to call `PUT /api/leads/[leadId]/sourcing-status` when changed.
       - Use `useRouter().refresh()` on success to update the UI.
       - Add a prominent badge showing the current `sourcingStatus`.
    2. Update `src/components/manufacturers/detail-sections.tsx`:
       - Add a "Sourcing Notes" section.
       - Implement an add note form with a `<textarea>`.
       - **Wire to API**: Use `fetch` to call `POST /api/leads/[leadId]/notes` on submit.
       - Render the list of notes (passed from the server-side query).
  </action>
  <verify>
    <automated>grep -E "sourcing-status|notes" src/components/manufacturers/detail-header.tsx src/components/manufacturers/detail-sections.tsx</automated>
  </verify>
  <done>Detail page fully wired to workflow APIs with persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Integration (Visibility & Filtering)</name>
  <files>src/app/(dashboard)/manufacturers/page.tsx, src/components/manufacturers/results-table.tsx, src/components/manufacturers/search-toolbar.tsx</files>
  <action>
    1. Update `src/app/(dashboard)/manufacturers/page.tsx`:
       - Extract `sourcingStatus` from `searchParams` (handling both string and string array).
       - Add it to the `queryParams` passed to `getManufacturers` and sub-components.
    2. Update `src/components/manufacturers/results-table.tsx`:
       - Add a new column "Sourcing" to display the `sourcingStatus` badge.
       - Ensure it's visible in both desktop table and mobile card views.
    3. Update `src/components/manufacturers/search-toolbar.tsx`:
       - Add `SOURCING_STATUS_OPTIONS` (Unqualified, Approved, Rejected, Flagged).
       - Add a new `FacetedFilter` for "Sourcing Status" linked to the `sourcingStatus` URL parameter.
       - Ensure the filter count and clear logic includes this new filter.
  </action>
  <verify>
    <automated>grep -E "sourcingStatus|FacetedFilter" src/app/(dashboard)/manufacturers/page.tsx src/components/manufacturers/results-table.tsx src/components/manufacturers/search-toolbar.tsx</automated>
  </verify>
  <done>Manufacturers dashboard displays sourcing status and allows filtering by it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sourcing workflow UI integration in dashboard and detail pages.</what-built>
  <how-to-verify>
    1. Go to the Manufacturers Dashboard.
    2. Verify the "Sourcing" column is visible and shows "Unqualified" for new leads.
    3. Use the "Sourcing Status" filter in the toolbar and verify results are filtered.
    4. Click into a manufacturer detail page.
    5. Change the sourcing status using the header UI and verify it persists after refresh.
    6. Add a note and verify it appears in the list with a timestamp.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → API | Mutation requests (Status/Notes) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Cross-Site Scripting (XSS) | Notes List | mitigate | React automatically escapes note content when rendering. |
</threat_model>

<success_criteria>
- Users can qualify manufacturers directly from the detail page.
- Sourcing status is visible at a glance in the main dashboard.
- Relationship notes provide a persistent history for procurement teams.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sourcing-workflow-notes/06-02-SUMMARY.md`
</output>
