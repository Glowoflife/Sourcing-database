---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/lib/queries/manufacturers.ts, src/lib/queries/manufacturers.test.ts, src/app/api/leads/[leadId]/sourcing-status/route.ts, src/app/api/leads/[leadId]/notes/route.ts, src/app/api/leads/[leadId]/sourcing-status/route.test.ts, src/app/api/leads/[leadId]/notes/route.test.ts]
autonomous: true
requirements: [CRM-03, CRM-04]
must_haves:
  truths:
    - "A separate sourcing_status enum (Unqualified, Approved, Rejected, Flagged) exists in the database."
    - "A lead_notes table exists, linked to leads."
    - "API endpoints for status updates and note creation are functional and validated with Vitest."
    - "getManufacturers supports filtering by sourcing status."
    - "Mutation functions exist in manufacturers.ts query layer."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "sourcingStatusEnum and lead_notes table definitions"
    - path: "src/lib/queries/manufacturers.ts"
      provides: "updateLeadSourcingStatus and createLeadNote mutations"
    - path: "src/app/api/leads/[leadId]/sourcing-status/route.ts"
      provides: "API endpoint for updating sourcing status"
    - path: "src/app/api/leads/[leadId]/notes/route.ts"
      provides: "API endpoint for adding notes"
  key_links:
    - from: "src/lib/queries/manufacturers.ts"
      to: "src/db/schema.ts"
      via: "drizzle queries"
---

<objective>
Implement the Sourcing Workflow Infrastructure (Schema, API, and Data Layer) to support procurement decision-making.

Purpose: Provide the backend foundation for qualifying leads and maintaining relationship notes (CRM-03, CRM-04).
Output: Schema extensions, mutation functions, validated API routes, and updated query layer with filtering support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/db/schema.ts
@src/lib/queries/manufacturers.ts
@src/lib/queries/manufacturers.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema Updates for Sourcing Workflow</name>
  <files>src/db/schema.ts, src/db/schema.test.ts</files>
  <behavior>
    - sourcingStatusEnum should contain: "Unqualified", "Approved", "Rejected", "Flagged".
    - leads table should have sourcingStatus column defaulting to "Unqualified".
    - lead_notes table should have leadId, content, and createdAt.
  </behavior>
  <action>
    1. Update `src/db/schema.ts`:
       - Define `sourcingStatusEnum` with ["Unqualified", "Approved", "Rejected", "Flagged"].
       - Add `sourcingStatus` to `leads` table.
       - Create `lead_notes` table (id, leadId, content, createdAt).
       - Add `leadNotes` relations to `leads` and `lead_notes` (leads.notes: many(leadNotes)).
    2. Create `src/db/schema.test.ts` to verify the presence of new fields and tables in the schema object.
    3. Run `npx drizzle-kit push` to update the database.
  </action>
  <verify>
    <automated>npx vitest run src/db/schema.test.ts</automated>
  </verify>
  <done>Database schema updated and verified via test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Data Layer Mutations & Filtering</name>
  <files>src/lib/queries/manufacturers.ts, src/lib/queries/manufacturers.test.ts</files>
  <behavior>
    - updateLeadSourcingStatus: Updates a lead's status in the DB.
    - createLeadNote: Inserts a new note for a lead.
    - getManufacturers: Accepts `sourcingStatus` array for filtering.
    - getManufacturerDetail: Includes `notes` (desc) and `sourcingStatus`.
  </behavior>
  <action>
    1. Update `src/lib/queries/manufacturers.ts`:
       - Implement `updateLeadSourcingStatus(leadId, status)`.
       - Implement `createLeadNote(leadId, content)`.
       - Update `getManufacturers` to support `sourcingStatus` filter.
       - Update `getManufacturerDetail` to fetch `notes` and `sourcingStatus`.
    2. Update `src/lib/queries/manufacturers.test.ts` to cover these new functions and filtering logic.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries/manufacturers.test.ts</automated>
  </verify>
  <done>Query layer enhanced with mutations and filtering, verified by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API Endpoints for Workflow and Notes</name>
  <files>src/app/api/leads/[leadId]/sourcing-status/route.ts, src/app/api/leads/[leadId]/notes/route.ts, src/app/api/leads/[leadId]/sourcing-status/route.test.ts, src/app/api/leads/[leadId]/notes/route.test.ts</files>
  <behavior>
    - PUT /api/leads/[leadId]/sourcing-status: Calls updateLeadSourcingStatus, returns 200.
    - POST /api/leads/[leadId]/notes: Calls createLeadNote, returns 201.
  </behavior>
  <action>
    1. Create `src/app/api/leads/[leadId]/sourcing-status/route.ts`:
       - Implement PUT handler using `updateLeadSourcingStatus`.
       - Validate input with Zod.
    2. Create `src/app/api/leads/[leadId]/notes/route.ts`:
       - Implement POST handler using `createLeadNote`.
       - Validate `content` is present.
    3. Create Vitest test files for both routes to verify request handling and status codes.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/leads</automated>
  </verify>
  <done>API routes implemented and verified with Vitest tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Status updates and note content sent to the server |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | API Routes | mitigate | Validate all incoming status strings and note content using Zod schemas |
| T-06-01-02 | Information Disclosure | Notes Query | mitigate | Ensure notes are only fetched for the requested lead |
</threat_model>

<success_criteria>
- Schema supports sourcing status and lead-specific notes.
- Mutation functions implemented in query layer for clean API routes.
- API endpoints for workflow transitions are functional and tested.
- Manufacturers query layer supports filtering by sourcing status.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sourcing-workflow-notes/06-01-SUMMARY.md`
</output>
