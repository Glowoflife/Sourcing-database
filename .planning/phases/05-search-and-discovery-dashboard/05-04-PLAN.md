---
phase: "05"
plan: "04"
type: execute
wave: 3
gap_closure: true
depends_on: ["05-02", "05-03"]
files_modified: [src/lib/queries/manufacturers.ts, src/components/manufacturers/results-table.tsx, src/components/manufacturers/search-toolbar.tsx]
autonomous: true
requirements: [CRM-01]
must_haves:
  truths:
    - "Pagination controls are visible and functional."
    - "Manufacturers are sorted by status priority, then alphabetically."
    - "Accessibility standards from UI-SPEC are met (aria-sort, aria-live)."
  artifacts:
    - path: "src/components/manufacturers/results-table.tsx"
      provides: "Pagination UI and aria-sort headers"
    - path: "src/components/manufacturers/search-toolbar.tsx"
      provides: "aria-live result count region"
  key_links:
    - from: "src/components/manufacturers/results-table.tsx"
      to: "src/lib/queries/manufacturers.ts"
      via: "URL page parameter"
---

<objective>
Close functionality and compliance gaps identified during Phase 5 verification.

Purpose: Fix pagination UI, ordering logic, and accessibility requirements to fully meet the `05-UI-SPEC.md` contract.
Output: A fully compliant and polished Search & Discovery Dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-search-and-discovery-dashboard/05-VERIFICATION.md
@.planning/phases/05-search-and-discovery-dashboard/05-UI-SPEC.md
@src/lib/queries/manufacturers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Fix Ordering Logic in Data Access Layer</name>
  <files>src/lib/queries/manufacturers.ts</files>
  <action>
    Update `getManufacturers` sorting logic to match `05-UI-SPEC.md` ("Default ordering" section).
    Instead of `orderBy(desc(leads.createdAt))`, use a custom SQL case statement for status priority:
    Priority 1: `Extracted`
    Priority 2: `Crawled`
    Priority 3: `Processing`
    Priority 4: `New`
    Priority 5: `Errored`
    Then, sort alphabetically by `leads.name` ASC.
  </action>
  <verify>
    <automated>grep -E "CASE|Extracted|Crawled|Processing|New|Errored|asc.*leads.name" src/lib/queries/manufacturers.ts</automated>
  </verify>
  <done>Ordering logic is updated to reflect priority and alphabetical sorting.</done>
</task>

<task type="auto">
  <name>Implement Pagination UI and Accessibility</name>
  <files>src/components/manufacturers/results-table.tsx, src/components/manufacturers/search-toolbar.tsx</files>
  <action>
    In `results-table.tsx`, add a pagination footer component with Next/Prev buttons and current page indicator (e.g., "Page 1 of 5").
    Ensure clicking Next/Prev updates the `?page=` URL parameter using Next.js navigation hooks.
    Add `aria-sort` attributes to the TanStack Table headers based on the current sorting state.
    In `search-toolbar.tsx`, ensure the result count ("{N} manufacturers") is wrapped in an `aria-live="polite"` region.
  </action>
  <verify>
    <automated>grep -q "aria-sort" src/components/manufacturers/results-table.tsx && grep -q "aria-live.*polite" src/components/manufacturers/search-toolbar.tsx && grep -q "?page=" src/components/manufacturers/results-table.tsx</automated>
  </verify>
  <done>Pagination UI is functional and accessibility attributes are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component → DOM | Screen reader output depends on correct aria attributes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Pagination UI | mitigate | Validate the `page` param bounds before fetching to prevent extremely large offsets |
</threat_model>

<success_criteria>
- Pagination controls work and sync with the URL.
- Manufacturers are sorted correctly by status and name.
- Screen readers announce result count changes and sort directions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-and-discovery-dashboard/05-04-SUMMARY.md`
</output>