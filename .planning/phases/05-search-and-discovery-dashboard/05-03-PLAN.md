---
phase: "05"
plan: "03"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/app/(dashboard)/manufacturers/[leadId]/page.tsx, src/components/manufacturers/detail-header.tsx, src/components/manufacturers/detail-sections.tsx]
autonomous: true
requirements: [CRM-02]
must_haves:
  truths:
    - "Manufacturer details display all technical profile data (Products, Locations, Contacts)."
    - "Two-column layout is responsive (sticks right rail on desktop, stacks on mobile)."
    - "Empty states render headings and placeholder copy per UI-SPEC."
  artifacts:
    - path: "src/app/(dashboard)/manufacturers/[leadId]/page.tsx"
      provides: "Manufacturer detail route"
    - path: "src/components/manufacturers/detail-header.tsx"
      provides: "Profile summary and status header"
    - path: "src/components/manufacturers/detail-sections.tsx"
      provides: "Technical data blocks"
  key_links:
    - from: "src/app/(dashboard)/manufacturers/[leadId]/page.tsx"
      to: "src/lib/queries/manufacturers.ts"
      via: "getManufacturerDetail call"
---

<objective>
Build the Manufacturer Detail Page to display comprehensive technical profiles.

Purpose: Provide a deep-dive view into a single manufacturer's products, locations, and contact info.
Output: A functional `/manufacturers/[leadId]` route with a two-column layout.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-search-and-discovery-dashboard/05-UI-SPEC.md
@.planning/phases/05-search-and-discovery-dashboard/05-01-SUMMARY.md
@src/lib/queries/manufacturers.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Manufacturer Detail Header and Shell</name>
  <files>src/app/(dashboard)/manufacturers/[leadId]/page.tsx, src/components/manufacturers/detail-header.tsx</files>
  <action>
    Create the dynamic route `/manufacturers/[leadId]`.
    Fetch data using `getManufacturerDetail` from 05-01.
    Implement the header: Name, Website link, Pipeline Status badge, Extraction Date, and "Back to Manufacturers" link.
    Setup the two-column grid layout (main column + sticky right rail) as per UI-SPEC.
  </action>
  <verify>
    <automated>grep -q "params.*leadId" src/app/(dashboard)/manufacturers/\[leadId\]/page.tsx && grep -E "Website|Status|Extracted" src/components/manufacturers/detail-header.tsx</automated>
  </verify>
  <done>Detail page shell and header are implemented.</done>
</task>

<task type="auto">
  <name>Implement Technical Profile Sections</name>
  <files>src/components/manufacturers/detail-sections.tsx</files>
  <action>
    In `detail-sections.tsx`, implement the following components:
    - Main column: Products & CAS (compact table), Locations (list with address), Source Coverage (links to original pages).
    - Sticky Right rail: Snapshot stats (lead source, counts), Industries Served (chips), Capacity (normalized MT/year or raw text), Contact info links (Email, Phone, WhatsApp).
    Handle empty states for each section (e.g., "No products extracted").
    Ensure the right rail remains sticky on desktop (>1024px).
  </action>
  <verify>
    <automated>grep -E "Products & CAS|Locations|Source Coverage" src/components/manufacturers/detail-sections.tsx && grep -E "Industries Served|Capacity|Contacts" src/components/manufacturers/detail-sections.tsx</automated>
  </verify>
  <done>All technical data sections are rendered with proper formatting and empty states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL → Component | `leadId` from URL used to fetch data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | `getManufacturerDetail` | mitigate | Ensure `leadId` is a valid number and only authorized leads are shown |
</threat_model>

<success_criteria>
- `/manufacturers/[leadId]` displays all extracted manufacturer data.
- Two-column layout functions correctly on desktop.
- Responsive single-column layout on mobile.
- All empty states conform to UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-and-discovery-dashboard/05-03-SUMMARY.md`
</output>
