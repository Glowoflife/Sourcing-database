---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/app/(dashboard)/manufacturers/page.tsx, src/components/manufacturers/search-toolbar.tsx, src/components/manufacturers/results-table.tsx]
autonomous: true
requirements: [CRM-01]
must_haves:
  truths:
    - "Users can filter by Industry, Location (state), Capacity, and Status."
    - "Search query matches against name, products, and CAS."
    - "Results table is responsive and linkable to details."
  artifacts:
    - path: "src/app/(dashboard)/manufacturers/page.tsx"
      provides: "Manufacturers listing route"
    - path: "src/components/manufacturers/search-toolbar.tsx"
      provides: "Search and filter controls"
    - path: "src/components/manufacturers/results-table.tsx"
      provides: "Data display with TanStack Table"
  key_links:
    - from: "src/app/(dashboard)/manufacturers/page.tsx"
      to: "src/lib/queries/manufacturers.ts"
      via: "Server Component data fetch"
---

<objective>
Build the Manufacturers List Page with advanced search and discovery capabilities.

Purpose: Allow users to browse and filter the manufacturers database using a high-density dashboard.
Output: A functional `/manufacturers` route with a data table and filter controls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-search-and-discovery-dashboard/05-UI-SPEC.md
@.planning/phases/05-search-and-discovery-dashboard/05-01-SUMMARY.md
@src/lib/queries/manufacturers.ts
@src/app/(dashboard)/leads/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Create Manufacturers Search Toolbar</name>
  <files>src/components/manufacturers/search-toolbar.tsx</files>
  <action>
    Implement a sticky toolbar component as per 05-UI-SPEC.
    Include a search input (debounced) and multi-select popovers for Industry, Location (state), and Status.
    Include a bucket selector for Capacity.
    Sync state to URL search parameters using `useRouter` and `useSearchParams` from `next/navigation`.
    Display active filter chips below the toolbar that can be individually removed.
  </action>
  <verify>
    <automated>grep -E "useSearchParams|useRouter" src/components/manufacturers/search-toolbar.tsx && grep -E "Industry|Location|Status|Capacity" src/components/manufacturers/search-toolbar.tsx</automated>
  </verify>
  <done>Toolbar component is functional and persists state to the URL.</done>
</task>

<task type="auto">
  <name>Implement Manufacturers List Page and Results Table</name>
  <files>src/app/(dashboard)/manufacturers/page.tsx, src/components/manufacturers/results-table.tsx</files>
  <action>
    Create the `/manufacturers` route as a Server Component. 
    Use `src/app/(dashboard)/leads/page.tsx` as an analog for the basic layout and table structure, but upgrade to TanStack Table for sorting and complex columns.
    Fetch data using `getManufacturers` from 05-01 based on URL search params.
    Implement `results-table.tsx` using `@tanstack/react-table` and the shadcn `table` UI.
    Columns: Manufacturer (link to detail), Products (preview), CAS (preview), Industry, Location, Capacity, Status.
    Implement responsive behavior: wide table on desktop (>768px), stacked cards on mobile (<768px).
    Add loading state using `skeleton.tsx`.
  </action>
  <verify>
    <automated>grep -q "Manufacturers" src/app/(dashboard)/manufacturers/page.tsx && grep -q "flex-col md:table-row" src/components/manufacturers/results-table.tsx</automated>
  </verify>
  <done>List page is live with high-density table and responsive views, following the leads dashboard pattern for shell consistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL → Component | Search params from URL used to fetch data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Cross-Site Scripting (XSS) | Results Table | mitigate | Ensure manufacturer names and chemical data are properly escaped by React/Next.js |
</threat_model>

<success_criteria>
- `/manufacturers` route loads and displays data.
- Search and filters correctly update the URL and the results list.
- Table remains sticky and responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-and-discovery-dashboard/05-02-SUMMARY.md`
</output>
